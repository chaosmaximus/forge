# v0.6.0 Pre-Iteration Deferrals — Decision Matrix

**Closed by P3-4 W1.42 (#203)** on 2026-04-27.

This document records the 10 deferred items that landed on the
v0.6.0 pre-iteration backlog before Wave D. Each row is annotated as
**permanently deferred** (won't ship in v0.6.0) or **fix-in-v0.6.0**
with a one-line rationale.

The default disposition is **permanent defer** — every item below was
already gated by an explicit "Out of scope" line in
`docs/superpowers/plans/2026-04-26-v0.6.0-polish-wave.md` §"Out of
scope" or §"P3-3.5 / P3-3.6 / P3-3.7 deferred backlog". The W1.42 walk
re-evaluates each in the context of the *current* HEAD (not Plan A's
original mid-2026-04 baseline) and either confirms the defer or lifts
the item back into scope.

| # | Item | Disposition | Rationale |
|---|------|-------------|-----------|
| 1 | **2A-4d.3 T17 CI bench-fast gate promotion** | **Permanent defer** | BLOCKED on GitHub Actions billing — the gate requires 14 consecutive green master runs of `bench-fast`, which costs minutes-per-run × 14. Not actionable in code; track for whenever GHA budget allows. Closure path: open `bench-fast` `continue-on-error: false` in `.github/workflows/bench-fast.yml`. |
| 2 | **longmemeval / locomo re-run** | **Permanent defer (v0.6.1+)** | External datasets (snap-research/locomo, longmemeval cleaned) not in the repo. Per polish-wave plan §3 ("longmemeval/locomo: deferred"). On-demand pattern documented at `docs/benchmarks/recall-probe-benches.md`; results archived at 2026-04-13. |
| 3 | **SIGTERM/SIGINT chaos drill modes** | **Permanent defer (v0.6.1+)** | Per polish-wave plan §"Out of scope". The graceful shutdown handler IS done (P3-2 W7 + W1.29 supervisor); chaos *drill modes* (mid-write SIGTERM, mid-spawn SIGINT) are an additive operator-tooling feature, not a correctness gate. The supervisor's drain-on-shutdown contract is already test-pinned. |
| 4 | **criterion latency benchmarks (`benches/` directory)** | **Permanent defer (v0.6.1+)** | Per polish-wave plan §"Out of scope". The kpi_events latency observability surface (Tier 2) covers production latency tracking; criterion micro-benchmarks would add a parallel benchmarking surface that duplicates kpi_events for marginal gain. Fold into v0.6.1 if a perf regression surfaces. |
| 5 | **Prometheus bench composite gauge** | **Permanent defer (v0.6.1+)** | Small addition (one new gauge family) but adds no observability that the existing `bench_run_completed` kpi_events + `forge-next observe --shape bench-run-summary` doesn't already cover for ops. Operators querying bench composite trends use `observe`; Grafana dashboards subscribe to `kpi_events`, not Prometheus, for bench data. |
| 6 | **Multi-window regression baseline (for 2C-2 auto-PR)** | **Permanent defer (v0.6.1+)** | Per polish-wave plan §"Out of scope". 2C-2's pairwise (current vs prior) regression detection is sufficient for the v0.6.0 release gate. Multi-window (current vs N-day rolling) would catch slower drifts but at the cost of harder-to-explain alerts. Re-evaluate after operator feedback on 2C-2. |
| 7 | **Manual-override label (for bench results)** | **Permanent defer** | Cosmetic tagging on results docs — documents which results were hand-curated vs auto-generated. Marginal value; the auto-generated results all carry commit SHAs in the bench harness output, so manual vs auto is already disambiguated by file content. |
| 8 | **P3-2 W1 trace-handler behavioral test gap** | **Permanent defer (v0.6.1+)** | Partial coverage was closed by P3-2 W2's resolver test suite (9 paths). Remaining gap: an end-to-end test that flips a session-scoped `context_injection` flag and asserts the resulting `compile_context_trace` output mirrors the override. The compile-time `match` exhaustiveness already catches the original bug class (handler dropping `inj`); the behavioral test is purely additive. Track as v0.6.1 hardening. |
| 9 | **Per-tenant Prometheus labels** | **Permanent defer** | Per polish-wave plan §"Out of scope". Single-tenant deployments don't benefit; multi-tenant deployments are a v0.7+ product surface. The current global-counter Prometheus families are correct for the v0.6.0 deployment model. |
| 10 | **OTLP timeline panel (Grafana)** | **Permanent defer (v0.6.1+)** | Per polish-wave plan §"Out of scope". OTLP traces flow into Jaeger via the existing `BatchSpanProcessor` (P3-2 W3); a Grafana timeline view would require Grafana's Tempo data source. The validation procedure at `docs/observability/otlp-validation.md` covers the manual operator path for v0.6.0; auto-rendered panel is v0.6.1 polish. |

## Summary

**0 of 10 items are lifted into v0.6.0 scope.** Each was already
deferred by an explicit Out-of-scope line in the predecessor plan;
the W1.42 walk confirms that disposition on each individually rather
than letting them ride forward by default.

**Path forward.** The 10 items collectively form the v0.6.1+ post-GA
backlog for the "depth" pass after the breadth-focused v0.6.0
release. Operator dogfood + Round 4 cc-voice feedback may reorder or
elevate specific items; the v0.6.1 plan document opens with this
list as its starting set.
