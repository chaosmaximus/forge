[package]
name = "forge-hud"
version = "0.2.0"
edition = "2021"
description = "Rust-powered statusLine HUD for Forge"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
