# Sample skill (clean fixture)

Run `forge-next health` for status.
Use `forge-next recall query` to search memories.
Use `forge-next health-by-project` to get per-project counts.

JSON example: `{"method":"recall","params":{"query":"test"}}`
JSON example: `{"method":"health_by_project","params":null}`
